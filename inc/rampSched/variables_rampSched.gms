* Variables for ramp search
Free variables
    v_searchRampObj "sum of errors for searching ramps"
    v_rampNetLoadAve(ramp)
;

Binary Variables
    v_rampNotch(notch, notch) "Location of ramp notches"
    v_rampSegment(ramp, t) "Location of ramp segments"
;
